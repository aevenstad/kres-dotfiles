# Copy this file to hosts/<hostname>.bash and adjust paths for that machine.
#
# The filename must match: hostname -s
# Example:
#   cp hosts/example-host.bash hosts/lab-node-01.bash

# export APPTAINER_CACHEDIR="/path/to/.apptainer"
# export NXF_APPTAINER_CACHEDIR="/path/to/containers"
# export NXF_SINGULARITY_CACHEDIR="/path/to/containers"

# export KRAKEN_DEFAULT_DB="/path/to/kraken-db"
# export KRAKEN2_DEFAULT_DB="/path/to/kraken2-db"
# export CENTRIFUGE_DEFAULT_DB="/path/to/centrifuge-db"

# path_prepend_if_dir "/path/to/custom/bin"
# path_prepend_if_dir "/another/custom/bin"
# export PATH

# if command -v keychain >/dev/null 2>&1; then
#   eval "$(keychain --quiet --eval id_ed25519_machine_specific 2>/dev/null)"
# fi
#
# alias seqp="cd /path/to/projects"
